name = "empire-goal-form"
main = "worker.js"
compatibility_date = "2024-01-01"

# Set these as secrets via: wrangler secret put TG_BOT_TOKEN
# and: wrangler secret put TG_CHAT_ID
# DO NOT put actual values here — use `wrangler secret put` command
